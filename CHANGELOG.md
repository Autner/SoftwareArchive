# 更新日志

本项目的所有显著变更都记录在此文件中。
版本号以 [VERSION.txt](VERSION.txt) 为唯一来源。

## [1.0.0] - 2026-08-30

**版本号自本版起重置为 1.0.0 开始**（合并原 1.1.0 / 1.1.1 的全部内容后重新发布）。

#### 新增

- **记录型软件（Record）**：第三种更新策略——只保存 `info.yml` 备忘，不保存安装包、源码和校验文件，用于记录"新设备 / 重装系统后需要重新下载"的软件（典型：商业软件）
- 收录向导新增 Record 分支：填写名称、平台、可选官网地址、标签和备注后直接写入档案库并重建索引，没有下载、Git 归档与使用手册确认环节
- 「修改更新策略」支持 Record → Fixed / Maintain：转换时按目标策略现场补齐安装包或源码，现有信息全部保留
- 「检查全部更新」「校验资源完整性」（含 `-Action Verify`）自动跳过 Record 条目；「更新已有软件」对 Record 条目给出修改指引
- 主菜单统计新增「仅记录」计数；条目导出兼容没有校验文件的 Record 目录；导入 Record 条目不再生成手册模板
- **Record → Maintain 转换支持同时下载最新 Release 安装包**：转换时查询仓库最新 Release，逐个选择资产并映射平台后下载归档，版本号自动填为该 Release——转换出的条目与直接收录的 Maintain 结构一致；仓库没有正式 Release 时仅归档源码，之后可在「更新已有软件」中补下载
- 单元测试扩展到 46 项（Record 策略读写、索引收录、搜索命中、校验跳过判定、Git 进度执行器）

#### 修复

- **Fixed 收录/更新时无法分批添加多个平台的安装包**：此前只弹出一次文件选择框，三个平台的安装包若在不同文件夹就无法一起归档。现所有本地安装包流程（收录、手动选择、Fixed 更新、Record 转 Fixed）均支持"继续添加更多安装包"多轮添加，累计归档
- **更新停在"使用手册确认"时版本看似没变**：版本号只在任务最终确认后才写入正式资源库（暂存保护机制），此前确认界面对此提示不足，用户完成下载后误以为更新已生效。现确认界面与暂存提示均明确说明"确认前正式库仍是旧版本"
- **完整性校验误报与报错**：`-Action Verify` 结果行的 `local` 用在顶层脚本块导致逐条报错、名称不显示；访达生成的 `.DS_Store` 元数据文件被计为 Unlisted 问题（只要用访达打开过 Packages 目录就会触发）。现修复并跳过 `.DS_Store`
- **单独按 Esc 每次卡顿 1 秒**：bash 3.2 的 `read -t` 只支持整秒，为区分"单独 Esc"与方向键转义序列必须等 1 秒。现引入随工具启动的按键解析助手（`sa-keys.pl`，macOS 自带 perl，零依赖），以 cbreak 模式接管终端并把按键解析为 token 经管道供给主脚本——Esc 响应从 1022ms 降到约 50ms，Enter/方向键毫秒级；无 perl 或非终端环境自动回退原逻辑
- **stdin 关闭后菜单循环 100% CPU 空转**：按键读取遇到输入流结束（EOF）时返回为空，上层循环立即重绘并再次读取，形成无限空转——失控进程曾持续发热数小时。现改为 EOF 时干净退出（退出码 130）
- **Record 转 Maintain 输入地址后界面"卡死"**：Git 克隆、更新镜像和生成 bundle 此前在命令替换里静默执行，大仓库克隆期间界面毫无反馈，仓库需要凭据时更会隐式等待输入导致永久挂起。现改为后台执行并实时显示 git 进度（如 Receiving objects 45%…）与已用时，同时禁止 git 隐式弹出终端凭据提示，缺凭据时快速失败并给出明确错误
- **软件数量多时菜单列表超出屏幕、看不到选中项**：所有选择菜单改为视口滚动——只渲染终端高度装得下的条目，上下两端显示「↑/↓ 向上（下）还有 N 项」指示，移动光标时视口跟随滚动，选中项永远可见；过长的帮助文本也会截断省略
- **删除软件时无论选谁都显示/只能删除排序第一个**：删除流程在同一行 `local` 语句里先赋值选中索引、再取软件名，而 bash 会先整体展开再赋值，索引用的是空值，空下标固定取到第一条。已拆分为两条语句；更新、策略转换等流程不受影响
- **收录 Record 软件后完成画面显示错误软件名**：完成画面在重建资源索引之后才读取软件名，而重建索引会重新扫描全部条目并覆盖该变量，导致显示成字母序最后一条（如录入 Chrome 显示成 WeChat）。目录、info.yml 与索引数据均不受影响，现已在重建前捕获软件名
- **bundle 校验不再依赖当前目录**：`git bundle verify` 现在优先在软件自己的 Git mirror 内执行；无 mirror 时用临时空仓库校验。此前从 App 启动时工作目录不是 git 仓库，所有 Maintain 软件的 bundle 校验都会误报失败
- **修复 bash 3.2 全角括号吞字**：变量展开紧跟全角括号时（如 `-Action Verify` 的结果行），括号及其后的内容会被整体丢弃；相关 8 处输出全部改为 `${VAR}` 花括号写法
- 校验资源完整性对无 bundle 的 Fixed 软件不再显示空括号
- **输入行在黑色背景下不可见**：输入框的提示与输入文字此前依赖终端默认文字色，浅色背景的配置改成黑底后（黑字黑底）会完全隐身；现改为显式灰色着色，浅色/深色背景均可读，Ctrl+C 提示同步处理
- README「界面预览」改为分隔线样式，避免中英文混排时边框错位

### 历史版本（旧版本号体系）

## [1.0.9] - 2026-08-30

#### 新增

- **搜索档案**：按关键字匹配名称/备注/标签/状态/使用手册全文，查看软件详情
- **检查全部更新**：一次检查所有 Maintain 软件的最新 Release，汇总有新版/已最新/失败三类结果，可一键进入更新向导
- **标记软件状态**：为软件标记 `active / deprecated / migrated` 生命周期状态；非 active 的软件不再参与更新检查，主菜单显示统计
- **维护源码镜像 (Maintain) 独立菜单**：不再只能从更新流程进入
- **导出 / 导入软件条目**：把单个软件打包成 `.saentry.zip` 分享给他人，对方一键入库（含安装包或仅信息与手册两种模式）
- **TSV 资源索引**：纯 bash 生成 `资源索引.tsv`（无 zip 依赖、机器可读），与 xlsx 并存；xlsx 生成失败不再阻断
- **`-Action List`**：命令行输出 TSV 清单到 stdout，供其他工具消费
- **并发锁**：交互菜单与批量检查互斥，防止两个实例同时写入档案数据；带陈旧锁自动回收
- **bundle 完整性校验**：`校验资源完整性` 现在会对 `Source/*.bundle` 运行 `git bundle verify`
- **单元测试框架**（`Work/Tests/run-tests.sh`，纯 bash）并接入 CI
- 仓库添加 MIT LICENSE

#### 变更

- 版本号单一来源化：运行时从 VERSION.txt 读取，不再散落多处
- 主菜单重组（17 项），顶栏新增“失效/迁移”统计
- README 增加“界面预览”与功能总览

## [1.0.8] - 2026-08-29

#### 变更

- 桌面应用改为“代码在 App 包内、数据在家目录”架构（`SA_DATA_HOME`），升级/卸载/重装 App 均不影响数据
- 便携文件夹模式（不设置 SA_DATA_HOME）行为不变

## [1.0.7] - 2026-08-29

#### 变更

- 桌面应用首次运行把工具安装到 `~/SoftwareArchive`，升级时仅同步代码文件、保留数据（过渡方案，已被 1.0.8 取代）

## [1.0.6] - 2026-08-29

#### 初始公开版本

- 软件收录/更新/删除向导、使用手册制度、SHA256 校验闭环
- GitHub Release 追踪（含私有仓库令牌支持）、断点续传下载
- Git mirror 与 `.bundle` 源码保全（Maintain）
- 自包含 macOS 应用打包与 CI/Release 工作流
